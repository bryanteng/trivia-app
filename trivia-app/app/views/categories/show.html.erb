<%= pluralize(@category.trivia.count , "#{@category.name} Question")  %> :

<% @category.trivia.where("id NOT in (?)", current_user.trivia.pluck(:id)).each do |trivia| %>
<li> <%= link_to(CGI.unescapeHTML(trivia.question), trivium_path(trivia)) %> </li>

<% end %>
